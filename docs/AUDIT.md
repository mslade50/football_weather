# football_weather — AUDIT

Date: 2026-08-23. Scope: current state of `C:/Users/McKinley Slade/dev/football_weather` and what is liftable from `C:/Users/McKinley Slade/dev/golf_scraping`. All paths absolute. Nothing here was modified.

---

## 1. Working tree inventory (football_weather)

| Path | Size / shape | Role | Fate |
|---|---|---|---|
| `C:/Users/McKinley Slade/dev/football_weather/app.py` | 2130 B | Streamlit landing page (product notes: wind volatile early week, dot size = under edge, rain suppressed until October, lines not real-time, times local) | Delete in Phase 4 |
| `.../pages/nfl_weather.py` | Streamlit page | Reads `nfl_weather.csv`; Plotly scatter_mapbox; dot rules; hover; sidebar tables | Rules ported to `pipeline/model/signals.py`; file deleted Phase 4 |
| `.../pages/cfb_weather.py` | Streamlit page | Reads `cfb_weather.xlsx` FBS sheet + backtest xlsx; DOW-dependent signal thresholds; backtest bucket match | Rules ported; deleted Phase 4 |
| `.../pages/combined_signals.py` | Streamlit page | Cross-league signal flags | Rules ported to preset filters; deleted Phase 4 |
| `.../requirements.txt` | streamlit, pandas, plotly, numpy, openpyxl, fsspec, streamlit-plotly-events, pytz | Streamlit deps | Replaced Phase 0 |
| `.../nfl_weather.csv` | 1 row (Super Bowl, Timestamp 2026-04-15T10:00:55) | Legacy output = column spec | Kept as spec fixture; regenerated by `pipeline/outputs/legacy.py` |
| `.../cfb_weather.xlsx` | sheets FBS 31x37, Other 8x24 (bowls, Timestamp 2025-12-29T10:01:36) | Legacy output = column spec | Same |
| `.../cfb_weather_backtest.xlsx` | Backtesting 118x17, Stadiums 129x4 | Backtest bucket grid + per-stadium under records | Replaced by `board/backtest.json` (Phase 6); kept as fixture |
| `.git` | 1641 commits 2024-09-16 -> 2026-04-15 | Contains recoverable artifacts (below) | — |

No generator code, no `.github/`, no workflow YAML has ever been committed. The pipeline ran from another repo/runner and pushed data files here.

## 2. Recoverable artifacts in git history

| Artifact | Commit | Command | Content |
|---|---|---|---|
| `cfb_locations_updated.csv` | `3aa1fa2` (also `25250b0`) | `git show 3aa1fa2:cfb_locations_updated.csv` | 658 rows x 34 cols: CFBD /teams export (Id, School, Mascot, Abbreviation, Alt Name1-3, Conference, Classification fbs/fcs/ii/iii, Location Venue Id/Name/City/State/Zip/Country/Timezone/Latitude/Longitude/Elevation(m)/Capacity/Year Constructed/Grass/Dome) + custom: Avg_temp (annual F), avg_wind_sep/oct/nov/dec (FBS only, 131 rows), wind_vol (High/Mid/Low). This is the CFB stadium DB the generator joined on. |
| `bol_ncaaf.db` | `25250b0` | `git show 25250b0:bol_ncaaf.db > bol_ncaaf.db` | sqlite `ncaaf_odds(Game 'Home Mascots vs Away Mascots', Timestamp, Spread, Home_odds, Away_odds, 'Total Points', Over_odds, Under_odds)`, 2208 rows, 41 snapshots 2024-09-15/16 every 15 min = BetOnline NCAAF scraper output |
| `fd_cfb.db` | `25250b0` | same | sqlite `totals('Event Name' 'Away @ Home', 'Total Points', 'Over Odds', 'Under Odds', timestamp)`, 5594 rows = FanDuel CFB totals scraper |
| NFL curated stadium table | every `nfl_weather.csv` version | `git log --format=%H -- nfl_weather.csv` then `git show <sha>:nfl_weather.csv`, concat, first-per-stadium | 23 stadiums: avg_wind, wind_vol, orient, wind_impact, weakest_wind_effect, game_loc, year_built, home_temp/away_temp per team (values in AUDIT §5) |
| CFB curated fields | every `cfb_weather.xlsx` version | same, per home team | orient, wind_impact, weakest_wind_effect for ~130 FBS home teams (NOT in cfb_locations_updated.csv) |
| Projection-feed rows | `d6f4fe6, 047f4be, fac1c2e, 2726b5b, 2af9168` (Sep–Oct 2024) | `git show 2af9168:cfb_weather.xlsx` | Only snapshots where Spread/Total_proj/My_total/Edge/My_spread/Edge_s are populated — golden rows for `model/fair.py` formulas |
| Full-week NFL sample | `6762d70` | `git show 6762d70:nfl_weather.csv` | 13-row in-season file |

Already extracted copies (scratchpad, session-local, may vanish): `C:/Users/McKinley Slade/AppData/Local/Temp/claude/C--Users-McKinley-Slade-dev/1731edc7-167a-4e7f-85d4-44526ee9116a/scratchpad/{cfb_locations_updated.csv,bol_ncaaf.db,fd_cfb.db,nfl_inseason.csv,cfb_inseason.xlsx,analyze_out.txt,analyze.py,analyze2.py}`. Phase 0 must re-derive these from git so nothing depends on the scratchpad.

## 3. Old pipeline behaviour (inferred from commits)

- 3 runs/day at 05:15, 10:00, 16:20 America/New_York, all 7 days; NFL csv committed first, CFB xlsx ~40 s later ("Update <file> with Timestamp <iso>"); author `McKinley <mckinleyslade@yahoo.com>` (runner git config).
- NFL runs year-round (carries last game row Jan–Aug); CFB only Sep–Dec (0 commits Jan–Aug). 2025 season start 2025-09-04.
- Timestamp = local ET without tz; UI labels it "EST" year-round.
- Openers = frozen first-seen values (Fd_open == FD_now in 36% of CFB rows); no history beyond open/now.
- Weather source = Open-Meteo hourly (evidence: wind_fg exact multiples of 0.0207124 = km/h->mph averaged over 3 hourly points; C->F float artifacts in temp_fg; rain_fg magnitudes to 22.2 = mm). Window = kickoff..kickoff+2h (3 hourly samples). CFB rounds wind 1dp / temp 2dp; NFL unrounded.
- Odds: CFB totals from FanDuel (Fd_open/FD_now + under prices Odds_o/Odds_n); CFB Open/Current = home spread (source unstated, likely FanDuel); NFL Spread/Total/Odds/Under from BetOnline (naming matches bol_ncaaf.db schema; Game key 'away vs home' lowercase city = BOL names minus mascots).
- Projection feed (Spread/Total_proj) dead since Oct 2024; My_total/Edge/My_spread/Edge_s NaN since.

## 4. Column specs (the contract `pipeline/outputs/legacy.py` must reproduce)

### 4.1 nfl_weather.csv (31 cols, in order)
`Game` ('away vs home', lowercase city: 'n.y. giants vs chicago', 'l.a. rams vs san francisco'), `Date` ('SUN 11/09'), `Time` ('01:00 PM' local kickoff), `stadium`, `avg_wind` (static mph, 0.0 domes), `wind_vol` (static low/mid/high/very high), `orient` (N-S, NW-SE, E-W, NE-SW; domes 'E'/'NE'), `wind_impact` (static low/med; NaN domes), `weakest_wind_effect` ('all','E/W','x N','x S','x N/W','S/E,N/W'), `game_loc` ('lat, lon'), `travel_alt` (home elev − away elev, m, int), `home_temp`, `away_temp` (annual avg F per team city), `year_built`, `wind_dir_1h`, `wind_dir_2h` (16-pt compass), `temp_fg` (mean F over window, unrounded), `wind_fg` (mean mph, unrounded), `wind_dir_fg`, `rain_fg` (sum mm), `gs_fg` (negative FRACTION e.g. -0.035), `away_fg` (fraction), `Spread_now`, `Odds_now`, `Total_now`, `Under_now`, `Spread_open`, `Odds_open`, `Total_open`, `Under_open`, `Timestamp` (ISO local ET, no tz).

### 4.2 cfb_weather.xlsx sheet FBS (37 cols, in order)
`Game` ('Away @ Home', CFBD School names: 'Miami (FL) @ Texas A&M'), `Date`, `Time`, `wind_vol` (High/Mid/Low), `orient`, `wind_impact` (Low/Med/High), `weakest_wind_effect` (case-inconsistent curated strings), `travel_alt` (float m), `home_temp`, `away_temp`, `wind_avg` (avg_wind_<game month>), `year_built`, `wind_dir_1h`, `wind_dir_2h`, `temp_fg` (2dp), `wind_fg` (1dp), `wind_dir_fg`, `rain_fg`, `gs_fg` (PERCENT, -6.5), `away_fg` (percent), `wind_diff` (= wind_fg − wind_avg), `game_loc`, `Fd_open`, `Odds_o`, `FD_now`, `Odds_n`, `Open`, `Current` (home spread, negative = home favored), `Spread`, `Total_proj`, `Move_t` (=(FD_now−Fd_open)/Fd_open), `Move_s` (= Open − Current), `My_total`, `Edge`, `My_spread`, `Edge_s`, `Timestamp`.
Sheet `Other` (24 cols): FCS/playoff games, Home Team/Away Team split, Game 'X vs Y', curated cols NaN, no odds; UI never reads it.

### 4.3 cfb_weather_backtest.xlsx
Sheet `Backtesting` (118 rows): Sport, Wind Above, Wind Below, Temp Above, Temp Below, Spread_l, Spread_h, CLV from Open (NaN/Positive/Negative), Wins, Losses, Push, Sample, Margin, ROI, '+ CLV', 'CLV %', Signal (row id). Sheet `Stadiums` (129 rows): Team, Stadium, Record 'W-L-P', Percentage. Matching logic in `pages/cfb_weather.py` (first match; Spread_h NaN never matches → NFL rows never match).

## 5. Reverse-engineered model (v1) — must be reproduced exactly

All components in percent; NFL output /100.
- wind(w=wind_fg): `<12→0; ≥12→2.0; ≥15→3.5; ≥17→6.5; ≥25→10.0` (17.46 yes/16.96 no; 25.2 yes/23.9 no)
- cold: `max(0, 30−temp_fg)*0.125`
- heat: `max(0, temp_fg−80)*0.125`
- rain(mm): `0 if month==9; <1→0; ≥1→1.5; ≥6→3.0; ≥20→6.5` (boundary between 5.1 and 6.6 unknown; 6.5 tier from 1 sample at 22.2)
- `gs_fg = −(wind+cold+heat+rain)`
- away_fg: heat_away = heat if temp_fg>80 and away_temp < cutoff (NFL 62–67 ambiguous → 65; CFB ≈54); cold_away = `max(0,32−temp_fg)*0.125` if temp_fg<32 and away_temp ≥ 65; alt tier on travel_alt m: NFL ≥1300→3.5, ≥900→2.0; CFB ≥1000→3.5 (930 → 0). `away_fg = −max(heat_away+cold_away, alt)` (tennessee@denver row shows override not sum).
- CFB derived (2024 only): `My_total = Total_proj*(1+gs_fg/100)`; `Edge=(FD_now−My_total)/My_total`; `My_spread = Spread*(1+away_fg/100)`; `Edge_s = Spread−My_spread`.
- UI signal rules: see ARCHITECTURE §7.4 (ported verbatim).

NFL curated stadium rows (name / avg_wind / wind_vol / orient / wind_impact / weakest): Acrisure 5.27/mid/NW-SE/med/'x N/W'; Arrowhead 10.48/mid/NW-SE/med/E/W; Bank of America 6.13/mid/NW-SE/med/all; Empower 8.37/high/N-S/low/'x N'; FedExField 9.08/mid/E-W/low/all; FirstEnergy 11.73/high/E-W/low/all; Gillette 6.23/high/N-S/med/'x S'; Hard Rock 11.49/high/NW-SE/low/all; Highmark 8.5/high/NW-SE/med/all; Lambeau 12.55/high/N-S/med/all; Levi's 8.05/low/NW-SE/low/E/W; Lincoln Financial 7.81/mid/N-S/low/E/W; Lucas Oil 0/mid/NE/None; Lumen 4.58/high/N-S/low/E/W; M&T 9.41/mid/E-W/low/all; MetLife 7.54/high/N-S/low/all; Nissan 7.58/mid/N-S/med/E/W; Paycor 6.98/low/NW-SE/med/'S/E,N/W'; Raymond James 9.23/very high/N-S/low/E/W; SoFi 0/mid/E/None; Soldier 13.02/very high/N-S/med/E/W; TIAA Bank 7.15/high/N-S/med/all; U.S. Bank 0/mid/E/None. (2024-era names; 9 more stadiums exist only in earlier csv versions — recover in Phase 0.)

## 6. Gaps that must be re-sourced

1. NFL stadium table exists only inside historical csv rows → `scripts/recover_static.py`.
2. CFB orient/wind_impact/weakest_wind_effect only inside historical xlsx rows → same script.
3. Team-name maps: CFB = CFBD School names; backtest Stadiums sheet uses 'UConn'/'FIU'; NFL = lowercase city ('n.y. giants','l.a. chargers') → `data/aliases/*.json`.
4. Projection feed (Total_proj/Spread) gone → replaced by Pinnacle-weighted devigged consensus (`model/fair.py`). Edge semantics change (documented in UI).
5. No workflow YAML anywhere → write from golf `board.yml`.
6. Timestamp tz-naive ET → new outputs stamp UTC + ET label; legacy writer keeps naive ET string.
7. rain_fg is mm (UI said inches) → keep mm; label correctly.
8. Static wind_vol/wind_impact/weakest_wind_effect are hand-curated with no source → keep as `*_static`, add computed replacements (ensemble wind_vol_fc, OSM orientation).

## 7. golf_scraping — what is reusable (by file)

Line counts: build.py 3262, state.py 539, worker/index.js 1156, telegram.py 120, base.py 184, betonline.py 487, betcris.py 638, fanduel.py 259, kalshi.py 816 (+kalshi_fill.py 144), novig.py 365, pinnacle.py 311, prophetx.py 603, draftkings.py 254, buckeye.py 560.

### 7.1 Copy verbatim (0–5 line edits)
| golf file | → football path | Edits |
|---|---|---|
| `golf_scraping/utils/telegram.py` | `utils/telegram.py` | keep `send_message`; rewrite `format_alert` |
| `golf_scraping/board/state.py` L40-50 `_load/_save`, L55-116 openers, L213-256 archive_last+scrape_baseline, L258-290 alerts (load_alerts/alert_sent/mark_alert/save_alerts), L294-391 change-only history | `pipeline/state.py` | rename player→side/team keys; ALERTS_CAP 200→500; HISTORY_CAP 60→120; add `schema_version` + `migrate()` |
| `golf_scraping/utils/state.py` (compute_delta/save_state/today_et) | `utils/state.py` | key = `game_id|market|book` |
| `golf_scraping/scrapers/base.py` | `pipeline/odds/base.py` | keep BaseScraper/scrape_with_retry/to_dataframe; add `GameLine` dataclass |
| `golf_scraping/board/d1_schema.sql` | `site/worker/migrations/0001_init.sql` | replaced by ARCHITECTURE §4 schema, same style |
| `golf_scraping/board/build.py` L150-170 + L891-910 (`american_to_prob/prob_to_american/american_to_decimal/_best/_median`), L911 `_retry`, L1887-1983 (`_d1_sql_value/_write_d1_deltas/_archive_rows`), L2255-2357 `_check_scrape_volume`, L2494-2502 `_alert_once`, L3196-3204 payload write loop `allow_nan=False`, L3220-3238 `push_to_r2` boto3 | `pipeline/build.py`, `pipeline/model/fair.py`, `pipeline/outputs/d1_out.py` | rename keys to `game_id|market|side|book` |
| `golf_scraping/main.py` | `main.py` | args `--book --sport --market --output --headed` |
| `golf_scraping/recon_betcris.py` | `scripts/recon_book.py` | URL + keyword params |
| `golf_scraping/board/gate_check.py` (shape only) | `pipeline/gate_check.py` | replace DataGolf with schedule kickoff horizon |
| `golf_scraping/board/worker/index.js` L1-30, L154-250 (Basic Auth, /data proxy), `boardIdentity`, `dispatchBoard/boardRunActive/ghHeaders/jsonResponse/notifyTelegram` (~L1060-1156) | `site/worker/index.js` | strip Kalshi/ProphetX order routes + pre-wednesday route; rename bucket/workflow |
| `golf_scraping/board/worker/wrangler.toml` | `site/worker/wrangler.toml` | name, bucket, D1 ids, crons (Quartz DOW 1=Sun; 3-cron empirical cap) |
| `golf_scraping/board/web/index.html` CSS shell, `app.js` fetch/poll/sort/filter/hover scaffolding | `site/web/` | new views |
| `golf_scraping/.github/workflows/board.yml` refresh job (checkout, setup-python 3.11 pip cache, setup-node 20, gate step, deps, `playwright install --with-deps chromium`, R2 state get loop, build, git commit retry loop, R2 put loop meta last, d1 execute gated by hashFiles, Telegram `if: failure()`), deploy job (`cloudflare/wrangler-action@v3`) | `.github/workflows/pipeline.yml`, `deploy.yml` | paths, names, scope input |
| `golf_scraping/.github/workflows/betonline.yml` | Playwright job B template | — |
| `golf_scraping/.github/workflows/sync-prophetx-secrets.yml` | optional | — |
| `golf_scraping/tests/test_betcris.py`, `test_novig.py`, `test_board_workflow.py` | test conventions | sys.modules stubs, AsyncMock, workflow-string contract tests |

### 7.2 Transport reusable, parser rewritten
| golf file | Transport kept | Football change |
|---|---|---|
| `scrapers/betonline.py` | chromium + `Stealth().apply_stealth_async`, goto betonline.ag, `page.evaluate(fetch)` to `api-offering.betonline.ag/api/offering/Sports/offering-by-league` with headers gsetting=bolsassite, utc-offset=240 | body `{Sport:'football', League:'nfl'|'nfl-preseason'|'college-football', filterTime:0}`; read `Game.AwayLine/HomeLine.{Spread,Total,MoneyLine}` (recon key names) |
| `scrapers/betcris.py` | httpx+BS4 `lines.bookmaker.eu` table.oddsTable, `vTeam_N/hTeam_N`, `vN_/hN_/vM_/hM_`, odds parser (EVEN/PK/½) | pages `/en/sports/football/nfl/`, `/nfl-preseason/`, `/college-football/`; cells `vS_/hS_` spread, `vT_/hT_` total; `th.oddsSubTitle` = neutral venue; `Game{N}_Time` title 'START 8/29 9:00am PT' |
| `scrapers/fanduel.py` | GET `sbapi.az.sportsbook.fanduel.com/api/content-managed-page?page=CUSTOM&customPageId=..&_ak=FhMFpcPWXMeyZxOx`, attachments envelope | `customPageId=nfl|ncaaf`; marketType `MONEY_LINE`, `MATCH_HANDICAP_(2-WAY)`, `TOTAL_POINTS_(OVER/UNDER)`; runner `handicap`, `result.type HOME/AWAY/OVER/UNDER`; competition-page `eventTypeId=1` |
| `scrapers/kalshi.py` + `kalshi_fill.py` | `_fetch_all_markets`, `_dollar_to_american`, fee 0.07·P(1−P), orderbook walk, 0.13 s throttle | `/events?series_ticker=KXNFLGAME|KXNFLSPREAD|KXNFLTOTAL|KXNCAAFGAME|KXNCAAFSPREAD|KXNCAAFTOTAL&status=open&with_nested_markets=true`; ladder → main line; use `*_dollars` fields (legacy int fields null) |
| `scrapers/novig.py` | `_gql` POST `api.novig.us/v1/graphql` w/ Origin/Referer | `league _in [NFL,NCAAF]`, `type Game`; markets `MONEY/SPREAD/TOTAL`, `is_consensus` = main, strike home-relative, `outcomes.available` prob |
| `scrapers/pinnacle.py` | guest arcadia `/sports/{id}/matchups` + `/markets/straight` join | sport 15; `market.type` spread/total/moneyline; period 0 |
| `scrapers/prophetx.py` | auth (API key / access+secret login), get_tournaments → get_sport_events → get_multiple_markets chunks | tournament filter 'American Football'/NFL/NCAAF; `market_types=moneyline,spread,total`; `selection.line` |
| `scrapers/draftkings.py` (optional) | curl_cffi impersonate chrome | eventGroup 88808 NFL / 87637 NCAAF, category 492 |

### 7.3 Not liftable
`board/build.py` merge/freeze/sim-fair logic (L188-1880, 2004-2227), `utils/events.py` (DataGolf), `board/freeze.py`, `utils/teetimes.py`, `utils/names.py` internals (keep approach only), `board/portfolio*.py`, `clv_report.py`, `sentinel.py`, Kalshi/ProphetX order-execution Worker routes, `board/reprice_bets.py` (imported but missing in golf repo — do not depend).

### 7.4 Lessons from `golf_scraping/ROBUSTNESS_AUDIT_2026-07-19.md` applied here
Stamp identity (game_id/season/week/run_id) on every row; never `continue-on-error` on R2 state fetch (NoSuchKey vs transient); push meta.json last; git commit before R2 push; concurrency group across all writing workflows; `git pull --rebase -X theirs --autostash` retry; CF cron Quartz DOW; GH cron backstop off-the-minute (`:17`); PAT 401/403 detection; dedup markers in R2 state only; `allow_nan=False`; clamp probs to finite (0,1); fail-open gate before Chromium install; thread league/sport through everything (no `tour='pga'`-style constants).

## 8. External sources verified (2026-08-23)
- Open-Meteo forecast/ensemble/historical-forecast/previous-runs/elevation: free, no key, 10k calls/day; multi-model + multi-location batching.
- NWS `api.weather.gov` `/points` + `/gridpoints/{wfo}/{x},{y}`: free, User-Agent required, ~7.5 d, gusts + PoP + QPF.
- nflverse `https://raw.githubusercontent.com/nflverse/nfldata/master/data/games.csv`: public, per-game roof/surface/stadium_id/location; `stadiums.csv` 404.
- CFBD `/venues`, `/games`, `/teams?division=fbs`: Bearer key (free tier).
- ESPN scoreboard/core venues: no key; venue id + neutralSite.
- Wikidata SPARQL P118/P115: coords, capacity, opened; no roof/elevation.
- OSM Overpass `leisure=pitch + sport=american_football` (around:400): orientation (Gillette 158°, AT&T 69°, Ohio 7°); rate-limit 2–5 s.
- Kalshi, Novig GraphQL, FanDuel sbapi, bookmaker.eu lines viewer: unauthenticated httpx works today. BetOnline direct API 403 (Playwright). ProphetX partner creds. The Odds API optional (500 free credits/mo; historical 10x for true openers).
- ToS: Kalshi Data ToS and FanDuel ToU forbid scraping/redistribution → site stays private; never republish raw prices.
